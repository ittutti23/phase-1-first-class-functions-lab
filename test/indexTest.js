require ( './helpers.js' );

describe("index.js", () => {
  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })

  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })


  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })

  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })


  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })


  it("has a function expression called divide", () => {
    expect(divide).to.exist
  })

  })